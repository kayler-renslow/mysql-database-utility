-fixed bug where reconnecting to a different server would just connect to the first connected server
-fixed bug where pressing disconnect button would requery data when it shouldnt
-renamed main class
-now whenever an error gets put into the console, it will pop up a window as well
-removed date picker column editor
-added very basic HTML editor