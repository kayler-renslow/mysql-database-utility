-renamed the query types since jdbc's executeQuery and executeUpdate methods weren't based upon ddl and dml specs
-fixed bug of menu item 'sync to database' being enabled when the table had no columns